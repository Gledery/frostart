## 改动说明

<!-- 一请描述清楚这个 PR 做了什么，比如：修复了 xxx bug / 抽离了 xxx 重复函数 / 新增了 xxx 功能 -->

## 影响范围

<!-- 列出涉及的主要文件 / 组件 / 函数，方便 review 时定位，比如：
- `js/widgets.js`：escapeHtml 移除，改为引用 `js/utils/dom.js`
- `js/sliders.js`：clampSliderValue 替换为 utils 中的 clamp
-->

## 关联 Issue

<!-- 如果有对应的 Issue，写上 `Closes #1` 或 `关联 #1`；没有就删掉这行 -->

## 测试情况

<!-- 简述你测了哪些功能点，如果你已经有把握没有影响可以不写。比如：
- [x] 搜索功能正常
- [x] 时钟显示正常
-->